{
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rimraf src/.observablehq/cache",
    "build": "observable build && cp -r dist/* ./",
    "dev": "observable preview",
    "deploy": "observable deploy",
    "observable": "observable"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.2",
    "@types/dotenv-parse-variables": "^2.0.3",
    "d3-dsv": "^3.0.1",
    "d3-time-format": "^4.1.0",
    "dotenv": "^16.4.7",
    "dotenv-extended": "^2.9.0",
    "dotenv-parse-variables": "^2.0.0",
    "puppeteer": "^24.10.2"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "prettier": "^3.5.2",
    "rimraf": "^5.0.5"
  },
  "engines": {
    "node": ">=18"
  }
}
